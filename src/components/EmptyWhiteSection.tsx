import React from 'react';

const EmptyWhiteSection = () => {
  return (
    <section className="w-full bg-white min-h-screen">
      {/* Huge empty white section - no content */}
    </section>
  );
};

export default EmptyWhiteSection; 